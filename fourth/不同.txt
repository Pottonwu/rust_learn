这两种方法的主要区别在于它们如何处理多态。第一种方法使用枚举和模式匹配来处理多态，而第二种方法使用 trait object 来处理多态。
使用枚举和模式匹配的方法更加灵活，因为你可以在模式匹配中添加更多的逻辑来处理不同类型的元素。
但是，这种方法也更加繁琐，因为你需要手动编写模式匹配代码。
使用 trait object 的方法更加简洁，因为你只需要定义一个 trait 并在不同类型中实现它即可。
但是，这种方法也更加局限，因为所有类型都必须实现相同的 trait。